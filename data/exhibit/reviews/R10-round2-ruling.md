# R10 Round 2, Chairman's Ruling

Spot-checks run before ruling: `ground-copy.json` confirms `ch0_5` and the a2 overture steps never set `boundary` (carry from ch3 `true` confirmed; same for ch9-ch11 after ch8). `CasesReroute.tsx:98/:103` confirmed stroking `var(--exh-rust)` with the "rust line" caption at line 62. `applyDerived(to)` confirmed synchronous at `StageController.tsx:187`. Grep for safari/webkit hits only the resolution file's own claim; `build-state.json` contains no record.

## VERIFIED (rollup)

Both round-1 p1s and nearly all p2s hold across all three lenses and both widths. The wall bar now ends at 1970 with a distinct hatched continuation and the S-row 2008 return draws at the correct axis position. The three flood stations are genuinely distinct cameras with a truthful 703-count and a clean final label. The mobile finale plates sit whole inside the frame and the grade-depth disclosure prints on phones with "equal" gone. Rust is ink-only on historical ledger years. The FLIP rebuild (inner-group tween, mid-flight retarget, single cleanup path) passes a live mid-flight probe; the press filter is a true inner shadow with a DOM-level assert; reduced-motion parity holds on every new rule. Township labels, memorial dignity, anachronistic label gating, PLSS water clip, Binga veil hole, hyde-p clip gating, neatline bleed, finale label suppression, climb column fill, annotation halos, and the full register-fixer p3 sweep all verify. The suite is materially harder (operand, flight, rust-year, resize, anchor-enumeration checks) and the 60-shot corpus exists at both widths.

## STILL OPEN

1. **orphan-square** (p2, round-1 item, fix failed). Confirmed in data: `boundary` is unset on `ch0_5` and the a2 overture/register steps (and ch9-ch11), so `boundary:true` carries from act 1 and the township fix made the stray ghost bolder. Fix: set `boundary:false` on `ch0_5` and the a2 overture stages, and clear or explicitly intend it for ch9-ch11.

2. **Rust on a historical figure** (p2, new, hard pigment law). `CasesReroute.tsx:98/:103` strokes the 1917-1948 exclusion route in rust and the caption calls it "the rust line". Fix: stroke with `var(--color-exh-red)`, reword the caption to "red line", and add this scene to the suite's rust-register assertion.

3. **WebKit pass unrecorded, now load-bearing** (p2). The resolution claims a recorded Safari walk; no record exists anywhere in the repo, and the round-2 FLIP move to an inner `g` makes the math newly dependent on `transform-box: view-box` (a WebKit interop soft spot) with all three frames on nonzero viewBox mins. Fix: run and record the WebKit/iOS walk in build-state including one within-frame flight (ch6 to ch5), and set `transform-box: view-box` explicitly on `[data-camera]`.

4. **Flight label pop, deferral clause skipped** (p2, round-1 sub-fix not implemented). `applyDerived(to)` runs at flight start so in-crop labels snap to destination counter-scale at t=0 (small entering ch5/a3-f1, oversized starting the pull-outs); the resolved-shot corpus is structurally blind to it. Fix: rest `[data-city-labels]` behind `data-tween` like the other micro-layers (or defer the `--gtext-k` write to settle).

5. **a6-sum phone tag** (p3 but the copy directs the reader to it). The accepted fix included scaling the ring and label at the phone breakpoint; only the halo shipped, and at 390px the EAST WOODLAWN 2026 tag is unreadable. Fix: scale the ring and label up at the phone breakpoint for a6-sum.

6. **Shot harness captures the wrong step on several mobile anchors** (p3, audit integrity). m-ch0_5, m-ch9, m-a2-takeaway evidence the previous step's stage, quietly blinding every screenshot pass. Fix: poll the engine's active-step attribute after scroll and assert shot id equals active step id before shooting.

7. **Ledger honesty** (p3, docs). The resolution records the safari walk as done in build state (it is not), claims the blanket ch4 anchor fix with no ch4 decline (the code deliberately exempts it), and cites an act-1 push cut record in design.md that does not exist; design.md also still describes the FLIP living on the svg element. Fix: one honest paragraph in the resolution and design.md recording the ch4 exemption, the act-1 cut, the corrected FLIP description, and the real WebKit-walk record once item 3 runs.

Downgraded, not blocking: marks badge severing NORTH LAWNDALE on two wide-cam shots, PLSS west-strip residual, sheetcut stale-attribute leak, camclip half-width math (latent, no corpus instance), lint dim-carry divergence, money-beat missing rust-cap assert, register-wall dock slice at the mobile anchor, f1 counter mid-count wrap, Next.js dev badge occlusion (dev artifact).

## RULING

**FIX-THEN-SHIP.** Nothing fundamental failed (the FLIP core, the corpus, and every round-1 p1 verify); what remains is a one-line data flag, a pigment swap, a cheap rest-rule, one recorded browser walk, and honest paperwork, none of which warrants a third full round.