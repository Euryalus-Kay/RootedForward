"use client";
/* R9 scene "CasesReroute". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function CasesReroute(_props: SceneProps) {
  return null;
}
