"use client";
/* R9 scene "Colophon". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function Colophon(_props: SceneProps) {
  return null;
}
