"use client";
/* R9 scene "Bridge". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function Bridge(_props: SceneProps) {
  return null;
}
