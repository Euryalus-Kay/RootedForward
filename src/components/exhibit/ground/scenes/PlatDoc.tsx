"use client";
/* R9 scene "PlatDoc". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function PlatDoc(_props: SceneProps) {
  return null;
}
