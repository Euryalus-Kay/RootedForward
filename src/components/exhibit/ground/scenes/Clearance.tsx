"use client";
/* R9 scene "Clearance". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function Clearance(_props: SceneProps) {
  return null;
}
