"use client";
/* R9 scene "advisory". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function AdvisoryGround(_props: SceneProps) {
  return null;
}
