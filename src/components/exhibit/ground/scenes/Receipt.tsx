"use client";
/* R9 scene "Receipt". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function Receipt(_props: SceneProps) {
  return null;
}
