"use client";
/* R9 scene "WellsClose". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function WellsClose(_props: SceneProps) {
  return null;
}
