"use client";
/* R9 scene "BaldwinBench". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function BaldwinBench(_props: SceneProps) {
  return null;
}
