"use client";
/* R9 scene "HansberryVoice". Wave-built; renders nothing until its build lands. */
import type { SceneProps } from "./registry";

export default function HansberryVoice(_props: SceneProps) {
  return null;
}
